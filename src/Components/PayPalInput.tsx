import React from "react";

const PayPalInput = () => {
  // const paypal = useRef();
  // useEffect(() => {
  //   window.paypal.Buttons({}).render(paypal.current);
  // }, []);
  return <div className="px-5">PayPalInput</div>;
};

export default PayPalInput;
